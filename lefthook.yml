pre-commit:
  piped: true
  commands:
    install:
      run: rustup component add rustfmt clippy
    fmt:
      run: cargo fmt
      stage_fixed: true
    clippy:
      run: cargo clippy --tests --fix --allow-dirty -- -D warnings
      stage_fixed: true
    test:
      run: cargo test
